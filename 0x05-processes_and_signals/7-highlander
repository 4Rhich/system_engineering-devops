#!/usr/bin/env bash
# This script displays its PID and handles SIGTERM signals
echo "$$"

trap 'echo "I am invincible!!!"' SIGTERM

while true
do
    echo "To infinity and beyond"
    sleep 2 &
    wait $!
done
