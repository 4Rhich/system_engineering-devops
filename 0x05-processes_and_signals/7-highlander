#!/usr/bin/env bash
# This script displays a message and catches SIGTERM signals
trap 'echo "I am invincible!!!"' SIGTERM

while true
do
    echo "To infinity and beyond"
    sleep 2 &
    wait $!
done
