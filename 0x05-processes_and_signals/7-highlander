#!/usr/bin/env bash
# This script displays its PID, a message in a loop, and handles SIGTERM
echo "$$" # Display the Process ID so the checker knows what to signal

# The trap needs to print the message AND then exit
trap 'echo "I am invincible!!!"; exit' SIGTERM

while true
do
    echo "To infinity and beyond"
    sleep 2
done
