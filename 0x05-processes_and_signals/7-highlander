#!/usr/bin/env bash
# This script catches the SIGTERM signal and prints a specific message

# Define the trap to echo the message when SIGTERM (signal 15) is received
trap 'echo "I am invincible!!!"' SIGTERM

# An infinite loop to keep the process running so it can receive signals
while true; do
    sleep 1
done
