#!/usr/bin/env bash
# This script displays its PID, a loop message, and traps SIGTERM
echo "$$" # This is the missing piece!

# Trap the signal and print the message
trap 'echo "I am invincible!!!"' SIGTERM

while true
do
    echo "To infinity and beyond"
    sleep 2 &
    wait $!
done
